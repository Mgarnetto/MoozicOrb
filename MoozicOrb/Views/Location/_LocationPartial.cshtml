@model LocationViewModel

<input type="hidden" id="page-signalr-context" value="@Model.SignalRGroup" />

<div class="location-hero">
    <h1 id="zone-location-name">@Model.Name</h1>
    <button class="btn-map-toggle" onclick="window.initGlobe()">View Map</button>
</div>

<div id="zone-map-container" class="map-drawer">
    <div id="chartdiv" style="width: 100%; height: 400px;"></div>
</div>

<div class="section">
    <h3>Top Artists in @Model.Name</h3>
    <div class="scroller">
        <div class="scroller-track">
            @* @foreach (var artist in Model.TopArtists)
            {
                <div class="card artist-card" onclick="window.AppRouter.navigate('/creator/@artist.Id')">
                    <div class="card-image" style="background-image: url('@artist.Pic')"></div>
                    <div class="card-info"><h4>@artist.Name</h4></div>
                </div>
            } *@
        </div>
    </div>
</div>

<div class="section">
    <h3>Local Buzz</h3>
    <div id="feed_@Model.SignalRGroup" class="feed-stream">
       @*  @foreach (var post in Model.Posts)
        {
            <partial name="_PostCard" model="post" />
        } *@
    </div>
</div>
